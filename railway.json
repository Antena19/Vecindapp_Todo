{
    "$schema": "https://railway.app/railway.schema.json",
    "build": {
        "builder": "NIXPACKS",
        "buildCommand": "pwd && ls -la && cd BACKEND && ls -la && cd REST_VECINDAPP && ls -la && dotnet restore REST_VECINDAPP.sln --verbosity detailed && dotnet build REST_VECINDAPP.sln --verbosity detailed"
    },
    "deploy": {
        "startCommand": "cd BACKEND/REST_VECINDAPP && dotnet run --project REST_VECINDAPP.csproj",
        "restartPolicyType": "ON_FAILURE",
        "restartPolicyMaxRetries": 10
    }
} 